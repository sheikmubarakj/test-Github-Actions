name: 'githubActions'

on: [push]

jobs:
  Job-1:
    runs-on: ubuntu-latest

    steps:
    - name: "Checking out my repo"
      uses: "actions/checkout@v6"

    - name: "Combined Step"
      run: | 
       echo "My First Step..."
       touch newfile.txt
       ls
